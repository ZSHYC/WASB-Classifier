# WASB-SBDT 项目文件结构说明

## 📁 项目整体架构

```
WASB-SBDT/
├── src/                    # 源代码目录（核心代码都在这里）
├── datasets/               # 数据集存放目录
├── pretrained_weights/    # 预训练模型权重
├── wasb_outputs/          # 输出结果目录
├── reports/               # 评估报告
├── main.py               # 主入口（已废弃，使用src/main.py）
├── README.md             # 项目说明
└── GET_STARTED.md        # 快速开始指南
```

---

## 🎯 核心代码目录：`src/`

### 1️⃣ **入口文件**

#### `src/main.py` ⭐ **最重要的文件**

- **作用**：整个项目的入口点
- **功能**：使用 Hydra 加载配置，选择并运行对应的 Runner
- **使用方式**：`python main.py --config-name=eval dataset=tennis_predict model=wasb ...`

---

### 2️⃣ **配置系统：`src/configs/`** 📝

所有配置文件都使用 YAML 格式，通过 Hydra 管理。

#### `configs/eval.yaml` - 评估主配置

- 默认配置，组合其他模块的配置

#### `configs/dataset/` - 数据集配置

- `tennis.yaml` - 网球数据集配置
- `tennis_predict.yaml` - 网球预测数据集配置（你正在用的）
- `soccer.yaml` - 足球数据集配置
- `basketball.yaml` - 篮球数据集配置
- `badminton.yaml` - 羽毛球数据集配置
- `volleyball.yaml` - 排球数据集配置

#### `configs/model/` - 模型架构配置

- `wasb.yaml` - WASB模型配置（基于HRNet）
- `tracknetv2.yaml` - TrackNetV2模型配置
- `deepball.yaml` - DeepBall模型配置
- `monotrack.yaml` - MonoTrack模型配置
- `ballseg.yaml` - BallSeg模型配置
- 等等...

#### `configs/detector/` - 检测器配置

- `tracknetv2.yaml` - TrackNetV2检测器配置
- `deepball.yaml` - DeepBall检测器配置

#### `configs/tracker/` - 跟踪器配置

- `online.yaml` - 在线跟踪器配置
- `intra_frame_peak.yaml` - 帧内峰值跟踪器配置

#### `configs/loss/` - 损失函数配置

- `hm_wbce.yaml` - 加权二分类交叉熵损失
- `hm_focal.yaml` - Focal损失
- `hm_bce.yaml` - 二分类交叉熵损失
- 等等...

#### `configs/transform/` - 数据变换配置

- `default.yaml` - 默认变换
- `crop.yaml` - 裁剪变换
- `color.yaml` - 颜色变换
- 等等...

#### `configs/runner/` - 运行器配置

- `eval.yaml` - 评估运行器配置
- `extract_frame.yaml` - 提取帧运行器配置

---

### 3️⃣ **数据集模块：`src/datasets/`** 📊

每个运动项目有独立的数据集类，负责加载和组织数据。

#### `datasets/__init__.py`

- **作用**：数据集工厂，根据配置选择对应的数据集类

#### `datasets/tennis.py` ⭐

- **作用**：网球数据集加载器
- **功能**：
  - 读取视频帧
  - 加载标注（CSV格式：x, y, visibility）
  - 组织训练/测试数据
  - 处理视频片段

#### `datasets/soccer.py` - 足球数据集

#### `datasets/basketball.py` - 篮球数据集

#### `datasets/badminton.py` - 羽毛球数据集

#### `datasets/volleyball.py` - 排球数据集

---

### 4️⃣ **数据加载器：`src/dataloaders/`** 🔄

负责将数据集转换为 PyTorch 可用的 DataLoader。

#### `dataloaders/dataset_loader.py`

- **作用**：构建 DataLoader
- **功能**：将数据集包装成 PyTorch DataLoader，支持批处理

#### `dataloaders/img_transforms.py`

- **作用**：图像变换
- **功能**：图像预处理（缩放、裁剪、归一化等）

#### `dataloaders/seq_transforms.py`

- **作用**：序列变换
- **功能**：处理多帧图像序列

#### `dataloaders/heatmaps/heatmaps.py`

- **作用**：生成热图
- **功能**：将球的位置标注转换为热图（用于训练）

#### `dataloaders/samplers/samplers.py`

- **作用**：采样器
- **功能**：控制数据采样策略

---

### 5️⃣ **模型模块：`src/models/`** 🧠

定义各种深度学习模型架构。

#### `models/__init__.py`

- **作用**：模型工厂，根据配置构建对应的模型

#### `models/hrnet.py` ⭐ **WASB的核心模型**

- **作用**：HRNet（High-Resolution Network）实现
- **功能**：WASB方法使用的骨干网络，多尺度特征提取

#### `models/unet2d.py`

- **作用**：U-Net 2D 实现
- **功能**：TrackNetV2 使用的模型

#### `models/deepball.py`

- **作用**：DeepBall 模型实现

#### `models/monotrack.py`

- **作用**：MonoTrack 模型实现

#### `models/ballseg.py`

- **作用**：BallSeg 模型实现

#### `models/resunet2d.py`

- **作用**：ResUNet 2D 实现
- **功能**：ResTrackNetV2 使用的模型

#### `models/segbase.py`

- **作用**：分割模型基类
- **功能**：提供 ResNet 骨干网络

#### `models/resnetv1b.py`

- **作用**：ResNet 变体实现
- **功能**：作为其他模型的骨干网络

---

### 6️⃣ **检测器模块：`src/detectors/``** 🔍

检测器负责运行模型推理并处理结果。

#### `detectors/__init__.py`

- **作用**：检测器工厂

#### `detectors/detector.py` ⭐

- **作用**：TrackNetV2 风格的检测器
- **功能**：
  - 加载模型
  - 运行推理
  - 调用后处理器
  - 返回检测结果

#### `detectors/deepball_detector.py`

- **作用**：DeepBall 专用检测器

#### `detectors/postprocessor.py`

- **作用**：后处理器（TrackNetV2风格）
- **功能**：从热图中提取球的位置坐标

#### `detectors/deepball_postprocessor.py`

- **作用**：DeepBall 专用后处理器

---

### 7️⃣ **跟踪器模块：`src/trackers/`** 🎯

跟踪器将多帧的检测结果连接成轨迹。

#### `trackers/__init__.py`

- **作用**：跟踪器工厂

#### `trackers/online.py` ⭐

- **作用**：在线跟踪器
- **功能**：
  - 连接相邻帧的检测结果
  - 预测球的位置（基于运动模型）
  - 处理遮挡情况

#### `trackers/intra_frame_peak.py`

- **作用**：帧内峰值跟踪器
- **功能**：在单帧内寻找峰值点

---

### 8️⃣ **运行器模块：`src/runners/`** 🏃

运行器是执行具体任务的核心，负责组织整个流程。

#### `runners/__init__.py`

- **作用**：运行器工厂

#### `runners/eval.py` ⭐ **你主要使用的文件**

- **作用**：评估运行器
- **功能**：
  - 加载数据集和模型
  - 运行推理
  - 执行跟踪
  - 计算评估指标
  - 生成可视化结果
  - 保存CSV预测结果

#### `runners/train_and_test.py`

- **作用**：训练和测试运行器
- **功能**：模型训练（目前未完全实现）

#### `runners/extract_frame.py`

- **作用**：提取帧运行器
- **功能**：从视频中提取帧图像

#### `runners/base.py`

- **作用**：运行器基类
- **功能**：提供通用功能

#### `runners/runner_utils.py`

- **作用**：运行器工具函数

---

### 9️⃣ **损失函数模块：`src/losses/`** 📉

定义各种损失函数用于模型训练。

#### `losses/heatmap.py` - 热图损失基类

#### `losses/bce.py` - 二分类交叉熵损失

#### `losses/wbce.py` - 加权二分类交叉熵损失（WASB使用）

#### `losses/focal_loss.py` - Focal损失

#### `losses/dice_loss.py` - Dice损失

#### `losses/combo_loss.py` - 组合损失

#### `losses/quality_focal_loss.py` - 质量Focal损失

#### `losses/segmentation.py` - 分割损失

#### `losses/ssd_loss.py` - SSD损失

---

### 🔟 **工具模块：`src/utils/`** 🛠️

各种工具函数和辅助类。

#### `utils/evaluator.py` ⭐

- **作用**：评估器
- **功能**：计算 Precision、Recall、F1、Accuracy、RMSE 等指标

#### `utils/dataclasses.py`

- **作用**：数据类定义
- **功能**：定义 Center、Track 等数据结构

#### `utils/image.py`

- **作用**：图像处理工具
- **功能**：图像变换、坐标转换等

#### `utils/heatmap.py`

- **作用**：热图处理工具
- **功能**：生成、处理热图

#### `utils/vis.py`

- **作用**：可视化工具
- **功能**：绘制检测结果、轨迹等

#### `utils/file.py`

- **作用**：文件操作工具
- **功能**：读取CSV、保存文件等

#### `utils/utils.py`

- **作用**：通用工具函数
- **功能**：创建目录、设置随机种子等

#### `utils/refine_gt.py`

- **作用**：标注精炼工具
- **功能**：优化真实标注数据

---

### 1️⃣1️⃣ **优化器模块：`src/optimizers/`** ⚙️

#### `optimizers/lr_schedulers.py`

- **作用**：学习率调度器
- **功能**：定义学习率衰减策略

---

### 1️⃣2️⃣ **工具脚本：`src/tools/`** 🔧

#### `tools/compare_detections.py`

- **作用**：比较检测结果
- **功能**：对比不同模型的检测结果

#### `tools/organize_comparisons.py`

- **作用**：组织比较结果
- **功能**：整理评估报告

---

### 1️⃣3️⃣ **设置脚本：`src/setup_scripts/`** 📦

#### `setup_scripts/setup_soccer.sh` - 设置足球数据集

#### `setup_scripts/setup_badminton.sh` - 设置羽毛球数据集

#### `setup_scripts/setup_basketball.sh` - 设置篮球数据集

#### `setup_scripts/setup_weights.sh` - 下载预训练权重

---

## 🔄 代码执行流程

### 典型评估流程：

```
1. main.py (入口)
   ↓
2. runners/eval.py (评估运行器)
   ↓
3. datasets/tennis.py (加载数据)
   ↓
4. dataloaders/dataset_loader.py (构建DataLoader)
   ↓
5. detectors/detector.py (运行模型推理)
   ↓
   models/hrnet.py (WASB模型)
   ↓
6. detectors/postprocessor.py (后处理，提取坐标)
   ↓
7. trackers/online.py (跟踪，连接轨迹)
   ↓
8. utils/evaluator.py (评估指标)
   ↓
9. 保存结果和可视化
```

---

## 📌 关键文件总结

### ⭐ 最常用的文件（按重要性排序）

1. **`src/main.py`** - 程序入口
2. **`src/runners/eval.py`** - 评估流程核心
3. **`src/detectors/detector.py`** - 检测器核心
4. **`src/models/hrnet.py`** - WASB模型实现
5. **`src/datasets/tennis.py`** - 数据集加载
6. **`src/trackers/online.py`** - 跟踪算法
7. **`src/utils/evaluator.py`** - 评估指标计算

### 📝 配置文件

- **`src/configs/eval.yaml`** - 主配置
- **`src/configs/dataset/tennis_predict.yaml`** - 你的数据集配置
- **`src/configs/model/wasb.yaml`** - WASB模型配置

---

## 💡 快速定位指南

- **想修改数据加载** → `src/datasets/` 和 `src/dataloaders/`
- **想修改模型架构** → `src/models/`
- **想修改检测流程** → `src/detectors/`
- **想修改跟踪算法** → `src/trackers/`
- **想修改评估指标** → `src/utils/evaluator.py`
- **想修改可视化** → `src/utils/vis.py` 和 `src/runners/eval.py`
- **想修改配置** → `src/configs/`

---

## 🎯 你现在使用的配置

根据你的代码，当前使用的配置是：

- **数据集**：`tennis_predict` (映射到 `Tennis` 类)
- **模型**：`wasb` (使用 HRNet)
- **检测器**：`tracknetv2` (默认)
- **跟踪器**：`online` (默认)
- **运行器**：`eval` (评估模式)

希望这个说明能帮助你快速理解项目结构！🎉
